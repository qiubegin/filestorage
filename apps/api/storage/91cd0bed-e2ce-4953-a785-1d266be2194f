# 全栈工程师笔试：Web 文件仓库
使用Fabr 与DeepSeek V4 Flash 在1–2 小时内协同完成支持多级目录、文件版本和文件移动的本地Web 文件仓库。

> Fabr 全栈工程师实践项目：Web 文件仓库

## 一、项目背景
团队需要一个简单、易用的Web 文件仓库，用于上传、组织、保存和管理文件。文件仓库需要支持文件夹和多级目录。用户可以在不同目录之间浏览、创建文件夹、上传文件，并将文件移动到其他目录。与普通文件列表不同，这个仓库还需要保留文件的历史版本：第一次上传一个文件时生成版本1；在本地修改该文件并再次上传到同一目录时，如果文件名相同，不创建新的文件记录，而是在原文件下生成下一个版本。请使用Fabr 和指定的AI 模型，与AI 协同完成这个项目。

## 二、项目目标
开发一个可以在本地运行的完整Web 应用，包括前端页面和后端服务。用户应当能够：
1. 查看文件仓库中的文件和文件夹；
2. 创建文件夹，并支持多级目录；
3. 进入不同目录并返回上级目录；
4. 将文件上传到当前目录；
5. 再次上传同名文件，并生成新的文件版本；
6. 查看一个文件的历史版本；
7. 下载当前版本或指定的历史版本；
8. 将一个文件移动到指定目录；
9. 删除文件；
10. 删除空文件夹。
请将它设计成一个真正可使用的小型产品，而不是只满足接口调用的演示页面。

## 三、目录规则
- 文件仓库存在一个根目录；
- 用户可以在根目录或任意文件夹下创建子文件夹；
- 文件夹可以形成多级目录；
- 同一个父目录下，不应出现两个同名文件夹；
- 不同目录下可以存在同名文件；
- 文件可以从当前目录移动到另一个指定目录；
- 不要求文件或文件夹重命名；
- 不要求移动文件夹；
- 只要求删除空文件夹，不要求递归删除非空文件夹；
- 文件移动到目标目录，而目标目录已经存在同名文件时，应阻止移动并清楚说明原因，不要求合并版本历史。

## 四、文件版本规则
文件版本按照“所在目录和文件名”识别：
- 第一次向某个目录上传文件时，生成版本1；
- 再次向同一目录上传同名文件时，在原文件下生成下一个版本；
- 同名文件在文件列表中只显示为一条逻辑文件记录；
- 文件列表应体现文件的当前版本及必要的基本信息；
- 用户可以查看文件的历史版本；
- 用户可以下载任意一个已保存的版本；
- 文件移动到其他目录后，其全部历史版本应一起移动；
- 移动文件不会产生新版本；
- 文件名发生变化时，视为另一个文件；
- 删除一个文件时，同时删除该文件的全部历史版本。
文件名大小写、特殊字符及其他边界情况如何处理，可以自行设计，但前后端行为必须保持一致，并在设计说明中说明。

## 五、用户体验要求
在整个设计和实现过程中，请尽可能保持良好的用户体验和友好的用户交互。我们不规定具体的页面结构和交互方式。你需要自行判断：
- 用户怎样快速理解当前所在目录；
- 用户怎样在多级目录之间导航；
- 文件和文件夹应该怎样区分；
- 用户怎样上传一个或多个文件；
- 上传、移动和删除等操作应如何触发；
- 用户怎样选择文件移动的目标目录；
- 文件版本历史如何呈现；
- 空目录、加载中、操作成功和操作失败时应显示什么；
- 危险操作是否需要确认；
- 耗时操作如何向用户提供反馈；
- 页面在不同窗口尺寸下应如何保持可用。
请重视信息层级、操作反馈、错误提示和状态一致性。不要只完成视觉静态页面，所有核心交互都应连接真实的后端API 并能够实际使用。我们不会仅按页面是否“好看”进行评价，更关注界面是否清楚、自然、高效，以及用户遇到异常情况时是否知道发生了什么、接下来应该怎么做。

## 六、后端与存储要求
所有文件和目录操作必须通过后端API 完成。本次项目只需要实现一种实际存储方式：将文件内容保存在服务器本地目录中。后端架构需要考虑未来替换或增加Git 仓库、OSS、S3 或其他远程文件服务的可能性，但不要求真正实现。存储方式发生变化时，文件版本、目录关系和文件移动等核心业务逻辑，以及对外API，应尽可能不需要改动。文件内容、目录结构和版本元数据都必须持久化。后端服务重启后，已经创建的目录、上传的文件及其历史版本仍然可用。

## 七、统一技术环境
项目提供已经初始化并可以运行的前后端工程。候选人不需要进行技术选型，也不建议替换现有框架。

### 基础环境
- Node.js 24 LTS
- TypeScript
- pnpm workspace
- 前后端Monorepo

### 前端
- React
- TypeScript
- Vite
- Tailwind CSS
- shadcn/ui
- TanStack Query
- Vitest
- React Testing Library

### 后端
- Node.js
- TypeScript
- Express
- Multer
- SQLite
- Prisma ORM
- Vitest
- Supertest

### 测试与质量检查
项目应预置以下命令：

```bash
pnpm dev
pnpm build
pnpm test
pnpm typecheck
pnpm lint
```

项目骨架只负责：
- 保证前后端可以启动；
- 提供数据库和本地文件存储所需的基础配置；
- 提供基础UI 组件；
- 提供测试和质量检查工具；
- 提供必要的环境变量示例。
项目骨架不应预先实现业务页面、文件API、目录模型、版本逻辑或存储抽象。这些属于候选人需要完成的内容。

## 八、范围说明
本次项目不要求实现：
- 用户登录和权限管理；
- 文件或文件夹重命名；
- 文件夹移动；
- 非空文件夹的递归删除；
- 在线编辑文件；
- 文件内容预览；
- 文件分享链接；
- Git 或对象存储的真实接入；
- 分片上传；
- 断点续传；
- 多服务器部署；
- 文件内容去重；
- 完整的生产级病毒扫描系统。
可以根据剩余时间增加体验或质量方面的改进，但首先应保证核心业务闭环完整、应用可以运行。我们不以功能数量作为主要评价标准，请自行判断在有限时间内什么最值得完成。

## 九、协作方式
本项目要求使用Fabr 和DeepSeek V4 Flash 正式版协同完成。可以让AI 参与需求分析、产品与UX 设计、技术方案、架构设计、前后端编码、测试、问题排查和文档编写。我们不要求候选人手工完成某个特定部分，也不以手写代码数量作为评价标准。候选人需要对最终结果负责，包括：
- 判断AI 给出的设计和实现是否合理；
- 向AI 提供清晰、准确的上下文和约束；
- 检查AI 生成的代码；
- 发现并修正错误；
- 运行真实测试；
- 确认文档与实际实现一致；
- 理解最终产品、接口和架构中的关键决策。

## 十、时间限制
建议完成时间为1–2 小时。请主动控制实现深度，不要为了增加功能而牺牲核心闭环和软件质量。无法完成全部内容时，应优先保证：
1. 项目能够启动；
2. 可以创建并浏览多级目录；
3. 可以在当前目录上传文件；
4. 同目录同名上传可以正确生成新版本；
5. 可以查看和下载历史版本；
6. 可以把文件移动到另一个目录；
7. 数据和文件能够持久化；
8. 关键业务规则有测试或其他可验证证据；
9. 已完成和未完成内容得到如实说明。

## 十一、交付要求
请提交一个可以在本地运行的完整项目，并提供一份README.md。README.md 至少应包括：
1. 项目启动和使用方法；
2. 已实现的功能；
3. 前后端主要模块及职责；
4. 文件和目录API 的设计说明；
5. 目录树、逻辑文件和文件版本的数据模型；
6. 同名文件生成新版本的处理方式；
7. 文件移动及目标目录冲突的处理方式；
8. 存储层如何替换为Git 或对象存储；
9. 测试内容、测试命令及实际测试结果；
10. 已考虑的安全、性能和异常处理问题；
11. 本次主动放弃或简化的内容；
12. 如果再增加两小时，准备优先改进什么。
设计说明可以由候选人和AI 共同完成，但内容必须与实际代码一致，候选人需要理解并能够解释其中的设计决策。

## 十二、完成标准
交付前，请至少确认：
- 前端和后端能够按照README.md 中的说明启动；
- 可以创建多级文件夹；
- 可以进入目录、返回上级目录并识别当前路径；
- 可以向当前目录上传文件；
- 再次向同一目录上传同名文件后会产生新版本；
- 同目录不会因为同名上传产生重复的逻辑文件；
- 不同目录可以保存同名文件；
- 可以查看文件版本历史；
- 可以下载当前版本和历史版本；
- 下载内容与对应版本一致；
- 可以将文件及其全部版本移动到另一个目录；
- 目标目录存在同名文件时，移动会被阻止并显示明确反馈；
- 可以删除文件及其全部历史版本；
- 可以删除空文件夹；
- 服务重启后，目录、文件和版本仍然存在；
- 项目包含可以运行的测试；
- build、test 和typecheck 的结果得到如实记录；
- 文档描述与实际实现一致。
